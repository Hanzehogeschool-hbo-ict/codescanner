'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var prosemirrorTables = require('prosemirror-tables');

/**
 * Predicate checking whether the selection is a [[`CellSelection`]].
 *
 * @param value - the value to check
 */
function isCellSelection(value) {
  return typeof value === 'object' && value instanceof prosemirrorTables.CellSelection;
}

exports.isCellSelection = isCellSelection;
Object.keys(prosemirrorTables).forEach(function (k) {
  if (k !== 'default' && !exports.hasOwnProperty(k)) Object.defineProperty(exports, k, {
    enumerable: true,
    get: function () { return prosemirrorTables[k]; }
  });
});
